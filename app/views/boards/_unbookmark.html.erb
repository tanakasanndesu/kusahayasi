<%= link_to board_bookmark_path(current_user.bookmarks.find_by(board_id: board.id), id: current_user.bookmarks.find_by(board_id: board.id).id), id: "unbookmark-button-for-board-#{board.id}", data: { turbo_method: :delete } do %>
  <i class="bi bi-star-fill"></i>
<% end %>
